package com.openclnica.webtest.enums;

/*
 * Enum class to ease the use of a switch for clickable
 * objects within a webpage.
 */
public enum Clickables {
	ID, CLASS_NAME, TAG_NAME, NAME, LINK_TEXT, PARTIAL_LINK_TEXT, CSS, XPATH
}
